import 'package:flutter/material.dart';

class PSWDPersonnelHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PSWD Personnel Home Screen'),
      ),
    );
  }
}
