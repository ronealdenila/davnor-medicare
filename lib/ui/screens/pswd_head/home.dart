import 'package:flutter/material.dart';

class PSWDHeadHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PSWD Head Home Screen'),
      ),
    );
  }
}
