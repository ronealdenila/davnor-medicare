import 'package:flutter/material.dart';

class ConsultationInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('Consultation Info Screen'),
        ),
      ),
    );
  }
}
